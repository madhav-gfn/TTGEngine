# Leave blank in local development.
# Set this in deployed frontend environments only when the backend lives on a different origin.
# Example: https://your-render-backend.onrender.com
VITE_API_BASE_URL=

# Optional: required only when backend ADMIN_API_KEY is configured.
VITE_ADMIN_KEY=
