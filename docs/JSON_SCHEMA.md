# JSON Schema

The full JSON schema specification for this project lives at the repository root in `JSON_CONFIGURATION_SCHEMA.md`.

The runtime validator used by the frontend is implemented in `Engine/Frontend/src/schemas/masterSchema.ts`.
