# Engine Architecture

The full architecture specification for this project lives at the repository root in `ENGINE_ARCHITECTURE.md`.

This implementation follows that document while mapping the runtime into the requested top-level structure:

- `Engine/Frontend`
- `Engine/Backend`
- `Games`
